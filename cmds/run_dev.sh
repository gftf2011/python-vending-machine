rm -rf docker-compose.yml && cat docker/dev/docker-compose.yml >> docker-compose.yml && rm -rf .env && cat ./env/dev/.env >> .env && chmod +x ./scripts/dev/postgresql/init.sh && docker-compose up -d --remove-orphans
